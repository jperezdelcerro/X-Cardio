<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/displayMessageActivity">
    <fragment
        android:id="@+id/splashFragment"
        android:name="com.example.x_cardio.fragments.SplashFragment"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_splashFragment_to_displayMessageActivity"
            app:destination="@id/displayMessageActivity" />
    </fragment>
    <activity
        android:id="@+id/displayMessageActivity"
        android:name="com.example.x_cardio.activities.DisplayMessageActivity"
        android:label="DisplayMessageActivity" />
</navigation>